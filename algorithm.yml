validate the inputs, number of inputs
validate the first arg, should be either create/delete
lopp through the arguments
get the instance_id
if create
  if instance_id is none, 
    it menas it is not create, so we will create
  else 
    instance_id is not none, it means its already running
  then update router53 records
else 
  if instance_id is none, 
    already destoryed
  else, 
    lets destroy the instance_id
